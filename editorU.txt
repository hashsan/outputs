＃これはエディターUの開発。
　この関数は、コンテントエディタブルがひとつあれば、それをキャッチし、即座に、データをジットハブに送るものである。
　これを更に推し進めて、ファイルを指定した場合に、保存できるようにする。
　インデックスは、別として、単純にエディタにファイル名を保存するものとなる。
　ん。保存したの？
　バーの挙動が微妙。昔のバーを参照してる？
　どうなってるのか。
　微妙にエディターUバグってるし。これでいけるのか。というか、クラスも何も関係ないんだな。これ。
　とてもすがすがしいし、潔い。
　もう、いっそのこと、URLのパラメータからロードするように変更しようか。







